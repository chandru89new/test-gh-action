name: Merge into Master
on:
  push:
    branches:
      - staging

jobs:
  merge-into-master:
    runs-on: ubuntu-latest
    steps:
      - name: setup git folder
        run: |
          git init
          git remote add origin https://x-access-token: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@github.com/$GITHUB_REPOSITORY.git
          git config --global user.name "Chandru"
          git config --global user.email "chandru89new@gmail.com"
      - name: rebase staging into master
        run: |
          git checkout -b master
          git pull origin staging --rebase
      - name: push to master
        run: |
          git push origin master
